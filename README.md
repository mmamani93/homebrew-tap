# PragmAI Homebrew tap

Install the standalone PragmAI command on macOS:

```sh
brew install mmamani93/tap/pragmai
```

After installation, use the private onboarding invitation supplied by the PragmAI administrator, then verify the local integration with `pragmai doctor`.
